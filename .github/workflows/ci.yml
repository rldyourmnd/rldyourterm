name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    name: Shell Script Linting
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Install ShellCheck
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends shellcheck

      - name: Run ShellCheck
        run: |
          find scripts -name "*.sh" -exec shellcheck {} \;

  powershell-syntax:
    runs-on: windows-latest
    name: PowerShell Script Syntax
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Parse PowerShell scripts
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $hasErrors = $false
          $scripts = Get-ChildItem -Path scripts -Filter "*.ps1" -Recurse

          foreach ($script in $scripts) {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile($script.FullName, [ref]$tokens, [ref]$errors) | Out-Null

            if ($errors.Count -gt 0) {
              $hasErrors = $true
              foreach ($errorRecord in $errors) {
                Write-Host "::error file=$($script.FullName)::$($errorRecord.Message)"
              }
            }
          }

          if ($hasErrors) {
            throw "PowerShell parsing failed for one or more scripts."
          }

  macos-flow-smoke:
    runs-on: macos-latest
    name: macOS Flow Smoke
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Validate macOS installer flow (dry-run)
        run: |
          ./scripts/macos/install.sh --dry-run

  linux-flow-smoke:
    runs-on: ubuntu-latest
    name: Linux Flow Smoke
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Validate Linux installer flow (dry-run)
        run: |
          ./scripts/install.sh --dry-run

  windows-flow-smoke:
    runs-on: windows-latest
    name: Windows Flow Smoke
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Validate Windows installer flow (dry-run)
        shell: pwsh
        run: |
          .\scripts\install-windows.ps1 -DryRun

  markdown-lint:
    runs-on: ubuntu-latest
    name: Markdown Linting
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Lint Markdown Files
        uses: DavidAnson/markdownlint-cli2-action@510b996878fc0d1a46c8a04ec86b06dbfba09de7 # v15
        with:
          globs: |
            **/*.md
            !.serena/**
            !node_modules/**

  link-check:
    runs-on: ubuntu-latest
    name: Link Checking
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Check Links
        uses: lycheeverse/lychee-action@2b973e86fc7b1f6b36a93795fe2c9c6ae1118621 # v1
        with:
          args: --no-progress README.md CONTRIBUTING.md SECURITY.md CHANGELOG.md ".github/**/*.md" "docs/**/*.md" "wiki/**/*.md"
