# macOS Scripts

Canonical macOS implementation.

## Entrypoints

- Full flow (auto): `./scripts/install.sh`
- Full flow (explicit): `./scripts/install-macos.sh`
- Dry-run: `./scripts/macos/install.sh --dry-run`
- Health: `./scripts/health-check-macos.sh --summary`
