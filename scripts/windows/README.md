# Windows Scripts

Canonical Windows implementation (PowerShell + WinGet).

## Entrypoints

- Full flow: `.\scripts\install-windows.ps1`
- Dry-run: `.\scripts\install-windows.ps1 -DryRun`
- Health: `.\scripts\health-check-windows.ps1 -Summary`
- Help: `.\scripts\install-windows.ps1 -Help`
