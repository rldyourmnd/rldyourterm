# v1.0.0 Release Pack (MVP closure sync)

Manual-only release checklist for v1.0 MVP closure (manual trigger with enforced preflight checks).

Req anchors for this checklist:
- `R-10`: no silent fallback; structured diagnostics + user-visible notice required.
- `R-11`: single-window baseline remains explicit and verifiable.
- `R-12`: release governance is manual-only and evidence-backed.
- `R-13`: Claude/Codex/Gemini compatibility is a hard release gate.

## 1) Documentation Lock

- `AGENTS.md` reflects locked priority order and manual governance.
- `planning/discovery/v1.0.0-answer-lock.md` remains aligned with ADR/quality/risk docs.
- `planning/system/traceability-matrix-v1.0.0.md` contains active links for `R-10`, `R-11`, `R-12`, `R-13`.
- `planning/operations/v1.0.0-manual-test-plan.md` defines the current MVP evidence flow:
  - profile logs (`MVP_HARNESS`, `MVP_HARNESS_CMD`, `MVP_STEP`, `MVP_RESULT`);
  - gate transcripts (`MVP_GATE_RESULT`, `MVP_MATRIX_RESULT`).

## 2) Validation Lock

- `bash planning/system/validate_planning.sh` -> `PASS`.
- Runtime checks:
  - `cargo check --workspace --locked`
  - `cargo test --workspace --locked`
  - `cargo test -p rldyourterm-app gpu_auto_fallback_emits_correlated_diagnostics_and_runtime_notice`
  - `cargo test -p rldyourterm-app forced_gpu_mode_reports_explicit_gpu_failure`
  - `cargo test -p rldyourterm-core stress_scrollback_cap_enforced_at_50k`
  - `cargo test -p rldyourterm-core byte_cap_trims_oldest_lines_when_budget_is_exceeded`
  - `cargo test -p rldyourterm-core oversized_line_does_not_evict_existing_history`
  - `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
  - `cargo fmt --all -- --check`
- Manual evidence is recorded per `planning/operations/v1.0.0-manual-test-plan.md`.

## 3) MVP Compatibility Lock (Claude/Codex/Gemini)

Required execution (with transcript capture):

```bash
bash scripts/mvp/scenario_claude.sh 4 | tee "scripts/mvp/output/gate-claude-$(date +%Y%m%d-%H%M%S).log"
bash scripts/mvp/scenario_codex.sh 4 | tee "scripts/mvp/output/gate-codex-$(date +%Y%m%d-%H%M%S).log"
bash scripts/mvp/scenario_gemini.sh 4 | tee "scripts/mvp/output/gate-gemini-$(date +%Y%m%d-%H%M%S).log"
```

Alternative aggregate gate:

```bash
bash scripts/mvp/run_matrix.sh 3 | tee "scripts/mvp/output/matrix-$(date +%Y%m%d-%H%M%S).log"
```

Evidence requirements:
- one profile runtime log per executed profile under `scripts/mvp/output/` (`<profile>-<timestamp>.log`),
- each profile log has `MVP_HARNESS` with `single_window_required=1` and `release_governance=manual-only`,
- each profile log has `MVP_HARNESS_CMD` with `--window-count 1`,
- each profile log has `MVP_STEP ... command=single-window:1 ...`,
- each profile log has `MVP_RESULT`,
- each `MVP_RESULT` contains `windows=1`,
- each `MVP_RESULT` contains `single_window_required=1` and `single_window_enforced=yes`,
- each `MVP_RESULT` contains `release_governance=manual-only`,
- each `MVP_RESULT` has acceptable runtime state (`running` or `degraded`),
- codex profile `MVP_RESULT` contains `gpu_retry_observed=yes` and `fallback_observed=yes`,
- gemini profile `MVP_RESULT` contains `cadence_resync_observed=yes`,
- each scenario transcript contains `MVP_GATE_RESULT ... status=pass ...`,
- matrix transcript contains `MVP_MATRIX_RESULT ... status=pass ...` when matrix mode is used.
- correlated fallback diagnostics/notice evidence is present via passing `gpu_auto_fallback_emits_correlated_diagnostics_and_runtime_notice` test output.
- forced-gpu fatal evidence is present via passing `forced_gpu_mode_reports_explicit_gpu_failure` test output.

## 4) Runtime Readiness Lock

- Stability scenarios pass without session drop.
- AI CLI compatibility scenarios pass for all three tools.
- Cadence re-sync and long-run smoke scenarios pass with monitor transfer evidence (`MVP_STEP ... command=transfer-cadence:*` + `cadence_resync_observed=yes`).
- Scrollback dual-bound evidence is present (`50_000` line cap + `512 MiB` byte budget with oversized-line non-eviction behavior).
- Single-window baseline is preserved in harness output (`windows=1`, `single_window_required=1`, `single_window_enforced=yes`).
- Manual-only release marker is preserved (`release_governance=manual-only`).
- Forced `gpu` mode failure path is explicitly fatal (no silent CPU fallback) in dedicated stability evidence.

## 5) Release Decision

- If any required artifact is missing or any required gate is failing: `release blocked`.
- If sections `1` through `4` are closed with evidence: status is `MVP closure ready`, and manual GitHub release preparation is allowed.

## 6) Latest Evidence Snapshot (2026-03-06 UTC)

- Documentation/consistency gate:
  - `bash planning/system/validate_planning.sh` -> `PASS` (2026-03-06).
- Runtime quality gate (workspace-level):
  - `cargo check --workspace --locked` -> `PASS` (2026-03-06).
  - `cargo test --workspace --locked` -> `PASS` (2026-03-06).
  - `cargo test -p rldyourterm-app gpu_auto_fallback_emits_correlated_diagnostics_and_runtime_notice` -> `PASS` (2026-03-06).
  - `cargo test -p rldyourterm-app forced_gpu_mode_reports_explicit_gpu_failure` -> `PASS` (2026-03-06).
  - `cargo test -p rldyourterm-core stress_scrollback_cap_enforced_at_50k` -> `PASS` (2026-03-06).
  - `cargo test -p rldyourterm-core byte_cap_trims_oldest_lines_when_budget_is_exceeded` -> `PASS` (2026-03-06).
  - `cargo test -p rldyourterm-core oversized_line_does_not_evict_existing_history` -> `PASS` (2026-03-06).
  - `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` -> `PASS` (2026-03-06).
  - `cargo fmt --all -- --check` -> `PASS` (2026-03-06).
- Compatibility gate artifacts:
  - matrix transcript: `scripts/mvp/output/matrix-20260306-052339.log`
  - profile logs:
    - `scripts/mvp/output/claude-20260306T052339Z.log`
    - `scripts/mvp/output/codex-20260306T052342Z.log`
    - `scripts/mvp/output/gemini-20260306T052344Z.log`
- Gate result lines (same run):
  - `MVP_GATE_RESULT profile=claude status=pass ...`
  - `MVP_GATE_RESULT profile=codex status=pass ...`
  - `MVP_GATE_RESULT profile=gemini status=pass ...`
  - `MVP_MATRIX_RESULT status=pass repeat=3 passed=3 failed=0 profiles=claude,codex,gemini ...`
- CI hardening and failure RCA:
  - `planning/operations/v1.0.0-ci-rca-2026-03-06.md`

Current run status: sections `1` through `4` are closed with evidence; release pack state is `MVP closure ready` for manual release preparation.
