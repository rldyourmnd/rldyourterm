# v1.0.0 Implementation Schedule (reset sync)

## Stage 0 — Planning Lock

- Confirm `AGENTS.md` and `answer-lock` alignment.
- Pass `bash planning/system/validate_planning.sh`.
- Freeze traceability and risk baseline.

## Stage 1 — Runtime Skeleton

- Create new self-authored workspace skeleton following VSA layers.
- Implement base session lifecycle contracts and event model.
- Implement shell resolver baseline (fish/starship with zsh fallback).

## Stage 2 — Render and Fallback Core

- Implement `cpu`, `gpu`, `auto` mode control.
- Add bounded GPU retry and deterministic fallback chain.
- Add monitor-driven cadence controller and transfer re-sync flow.

## Stage 3 — AI CLI Compatibility Hardening

- Run dedicated long-session scenarios for:
  - Claude Code
  - Codex
  - Gemini CLI
- Stabilize prompt/input/output timing and copy/paste behavior.

## Stage 4 — Stability and Latency Hardening

- Long-run stress passes.
- Memory bound validation (scrollback cap + growth trend).
- Failure-injection checks for GPU/PTY fault paths.

## Stage 5 — Release Lock

- Complete acceptance matrix evidence.
- Complete release-pack checklist.
- Produce manual release artifacts.

## Parallel Execution Reference

- Detailed 10-agent orchestration plan:
  - `planning/roadmap/v1.0.0-parallel-agent-execution-plan.md`

## Stage Exit Rule

Stage can close only when required evidence is recorded in manual test and release docs and traceability links remain valid.
