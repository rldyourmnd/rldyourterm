# Agent 07 — Feature Render CPU

## Scope (only)
- `crates/features/render_cpu/src/lib.rs`

## Goal
Build stable CPU renderer baseline for fallback and low-risk runtime continuity.

## Requirements
- Req: R-01, R-08
- deterministic output path and bounded memory behavior.

## Done
- CPU renderer API integrated with core state inputs.
- tests for basic render path behavior.
