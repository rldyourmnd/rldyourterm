# v1.0.0 MVP Closure Wave-3 Plan (10 Parallel Agents)

Date: 2026-03-02
Status: Ready for execution
Objective: close remaining MVP quality gaps and harden runtime contracts with zero known crash-path regressions.

Hard priorities (strict order):
1. Stability first (session continuity, crash-intolerant runtime)
2. AI CLI compatibility (Claude Code, Codex, Gemini CLI)
3. Speed/latency consistency (monitor-driven cadence, bounded runtime behavior)

## Context7-backed dependency anchors
- `portable-pty`: `/websites/rs_portable-pty` (`openpty`, `spawn_command`, `take_writer`, `try_clone_reader`, `wait/kill` lifecycle)
- `winit`: `/websites/rs_winit_winit` (`request_redraw`, `WindowEvent::RedrawRequested`, resize/scale/monitor event handling)
- `wgpu`: `/websites/rs_wgpu` (`SurfaceConfiguration`, `desired_maximum_frame_latency`, `SurfaceError` policy)

## Wave-3 Workstreams

1. Agent01 - Window contract migration (remove deprecated compatibility path)
- Scope: `crates/foundation-platform/src/window.rs`
- Deliverables:
  - switch implementation to `WindowFactory`/`WindowControl` contract path,
  - remove deprecated API usage from runtime path,
  - preserve monitor-transfer cadence re-sync signals.
- Req: `R-03`, `R-04`, `R-06`.

2. Agent02 - PTY boundary hardening pass
- Scope: `crates/foundation-platform/src/pty.rs`
- Deliverables:
  - strengthen closed/wait/kill edge semantics,
  - deterministic writer lease behavior,
  - no panic on lifecycle race-like sequences.
- Req: `R-01`, `R-09`.

3. Agent03 - Clipboard degrade-safe behavior hardening
- Scope: `crates/foundation-platform/src/clipboard.rs`
- Deliverables:
  - stable fallback behavior under backend failures,
  - consistent health-state transitions,
  - deterministic `set/get/clear` behavior in degraded mode.
- Req: `R-01`, `R-10`.

4. Agent04 - Core ingest resilience under burst inputs
- Scope: `crates/core/src/{events.rs,parser.rs,state.rs}`
- Deliverables:
  - bounded handling for burst/malformed sequences,
  - deterministic degrade events,
  - stronger tests for long input segments.
- Req: `R-01`, `R-08`.

5. Agent05 - Session policy deterministic escalation tests
- Scope: `crates/services/src/session.rs`
- Deliverables:
  - tighten recoverable/fatal escalation behavior,
  - preserve shell continuity when recoverable,
  - increase deterministic transition coverage.
- Req: `R-01`, `R-07`, `R-09`.

6. Agent06 - Render control and cadence contract hardening
- Scope: `crates/services/src/{render_mode.rs,render_pacing.rs}`
- Deliverables:
  - stronger no-silent-fallback guarantees,
  - explicit transition metadata consistency,
  - monitor transfer/re-sync edge case tests.
- Req: `R-02`, `R-03`, `R-04`.

7. Agent07 - GPU recovery policy edge coverage
- Scope: `crates/features/render_gpu/src/lib.rs`
- Deliverables:
  - deterministic classify/recover behavior for all supported errors,
  - robust reconfigure retry/degrade decisions,
  - strict tests for policy state transitions.
- Req: `R-02`, `R-03`.

8. Agent08 - CPU renderer bounded delta/frame behavior
- Scope: `crates/features/render_cpu/src/lib.rs`
- Deliverables:
  - deterministic stats under repeated delta renders,
  - bounded row/byte/cell accounting,
  - additional tests for no-growth edge conditions.
- Req: `R-01`, `R-08`.

9. Agent09 - Settings/shell/diagnostics integration maturity
- Scope: `crates/features/{settings,shell_integration,diagnostics}/src/lib.rs`
- Deliverables:
  - deterministic settings outcomes and parser edge handling,
  - shell diagnostics consistency and fallback observability,
  - typed diagnostics payload coverage.
- Req: `R-05`, `R-07`, `R-10`, `R-13`.

10. Agent10 - App MVP harness + docs synchronization
- Scope:
  - `crates/app/src/main.rs`
  - `scripts/mvp/*`
  - `planning/operations/v1.0.0-manual-test-plan.md`
  - `planning/operations/v1.0.0-release-pack.md`
  - `planning/quality/v1.0.0-acceptance-matrix.md`
  - `README.md`
  - `planning/roadmap/v1.0.0-agent-run-status-wave3.md`
- Deliverables:
  - stronger executable MVP gate scripts,
  - synchronized release/manual test evidence flow,
  - explicit compatibility gate outputs for Claude/Codex/Gemini.
- Req: `R-11`, `R-12`, `R-13`.

## Integration policy
- Parallel execution is allowed only with scoped-file ownership.
- Backward-compatible behavior is mandatory unless explicitly justified.
- Coordinator performs final convergence, checks, and conflict resolution.

## Done gate for Wave-3
- all 10 agents produce exit code `0`;
- `cargo check --workspace` passes;
- `cargo test --workspace` passes;
- `bash scripts/mvp/run_matrix.sh 2` passes;
- planning validation remains green.
