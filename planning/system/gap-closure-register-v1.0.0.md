# Gap Closure Register (v1.0.0 reset sync)

| Gap ID | Problem | Resolution | Status |
|---|---|---|---|
| G-001 | Legacy docs referenced removed `metrics/version/*` layer after repository reset. | Precedence and governance moved to planning/system layer; all mandatory references updated. | Closed |
| G-002 | Traceability contained stale `crates/*` implementation links after code reset. | Traceability now points only to active planning contracts and evidence artifacts. | Closed |
| G-003 | Validation script searched non-existent `metrics` path and produced false failure. | `validate_planning.sh` updated to planning/AGENTS/README scope with stable markers. | Closed |
| G-004 | Product goal wording was weaker than new vision (crash-intolerant + AI CLI-first + ultra-low latency). | Updated AGENTS, answer-lock, quality gates, acceptance matrix, roadmap and readiness docs. | Closed |
| G-005 | Self-authored-first policy was implicit and inconsistent across docs. | Policy formalized in AGENTS, discovery lock, stack contracts, risk and traceability artifacts. | Closed |
| G-006 | Requirement IDs drifted: acceptance referenced undefined `R-16`, and validator enforced only `R-01..R-12` despite active `R-13/R-14`. | Acceptance and quality docs aligned to `R-01..R-14`; validator updated to enforce `R-01..R-14` and reject unexpected Req IDs in authoritative docs. | Closed |
| G-007 | Manual test and readiness docs used stale `run_profile` examples with `repeat=2` and old cadence command names after harness hardening (`repeat >= 3`, monitor-transfer cadence token). | Updated operations docs to `repeat >= 3`, migrated cadence examples to `transfer-cadence:*`, and aligned readiness checks with workspace-level quality gate commands. | Closed |
| G-008 | Wave-5 backlog high/critical runtime items remained open for PTY recovery, GPU fallback observability, cadence resync, runtime palette, and shell baseline determinism. | Implemented wave-6 runtime convergence commits (`7526520`..`8d9096f`) with passing package-level checks/tests and explicit requirement references (`R-01..R-13`). | Closed |
| G-009 | VSA dependency direction and diagnostics envelope contract were still inconsistent (`app/ui -> core`, feature diagnostics missing canonical foundation envelope fields). | Removed direct `core` dependency from `app/ui` via `services` re-export and aligned feature diagnostics to canonical foundation envelope (`kind/severity/layer/correlation_id`) in commit `8d86b98`. | Closed |
| G-010 | `foundation-platform` window adapter was implemented, but app runtime used direct `winit` window ownership path. | Closed after runtime ownership migration: (1) app runtime window lifecycle is routed through `WindowFactory/WindowControl`; (2) cadence timing is sourced from foundation window contracts/events (`current_monitor_timing` + `DisplayRefreshChanged` path); (3) single-window/palette/fallback behavior parity is preserved under existing MVP gate evidence (`R-11..R-13`). Regression guard added in `planning/system/validate_planning.sh`: direct app-owned `window.request_redraw`, `window.set_title`, `window.current_monitor` patterns fail validation. | Closed |
| G-011 | Docs and Serena memories drifted from actual crate map and palette scope (stale planned layout terms and implicit free-form palette UX). | Synced architecture/settings docs and relevant memories to actual workspace structure; palette scope is now explicitly shortcut-first with no free-form UI command line. | Closed |
| G-012 | Runtime still had two consistency breaks: (1) deferred GPU init could continue on CPU path without explicit transition in `auto` mode; (2) `app` retained direct `core` dependency and GPU failure taxonomy leaked from `foundation` into render orchestration. | Closed via consistency hardening pass (2026-03-06): bounded deferred GPU init retry + deterministic `auto` fallback observability path added in `crates/app/src/gui_runtime.rs`; forced `gpu` mode now exits explicitly on init exhaustion; direct `rldyourterm-app -> rldyourterm-core` dependency removed; `GpuFailureKind` ownership moved into `services::render_mode`; `render_gpu` switched to services-owned failure taxonomy. | Closed |
| G-013 | Long-run memory/VRAM control and CI noise still had consistency gaps: scrollback retained padding and heap capacity, glyph cache growth was unbounded, PTY->GUI output path could backlog unboundedly, viewport resize allowed OOM-scale dimensions, GPU cell buffers could keep VRAM high-water after burst, and CI logs had avoidable warning/failure churn (`rust-cache` lockfile warning, Scorecard/SARIF/permissions drift, transitive unmaintained advisory noise). | Closed via hardening pass (2026-03-06): (1) scrollback cap enforced at 50_000 with trailing-space compaction and explicit shrink on clear; (2) glyph cache is bounded with deterministic eviction/fallback behavior; (3) PTY->GUI output path moved to bounded sync-channel with backpressure and coalesced `OutputReady` signaling (no unbounded event fanout), then further optimized with bounded coalesced parser-feed batching and per-tick bounded drain continuation to keep UI latency stable under AI burst output; (4) viewport geometry is clamped by axis and total-cell safety caps before terminal/grid/GPU realloc paths; (5) GPU cell buffers implement sustained-underutilization shrink with explicit old-buffer `destroy()` and forced full upload on realloc, and CPU fallback now releases GPU backend resources deterministically; (6) clipboard fallback cache is capped to 64KiB with UTF-8 boundary safety; (7) CI workflows updated for `Swatinem/rust-cache@v2.8.2`, strict permissions split, pinned action SHAs, and a stable Scorecard SARIF normalization/upload path that avoids OSSF publish-path workflow verification failures; (8) transitive-only advisory `RUSTSEC-2024-0436` is explicitly tracked via `.cargo/audit.toml` allowlist until upstream dependency chain provides a maintained replacement. | Closed |
| G-014 | PTY burst coalescing in GUI runtime could feed payloads larger than core ingest cap (`MAX_FEED_BYTES_PER_CALL`), creating deterministic data-loss risk under sustained AI CLI output bursts; CI still spent unnecessary cycles on docs/planning-only commits and compiled `cargo-audit` from source each run. | Closed via optimization pass (2026-03-06): (1) GUI runtime now splits coalesced payload into core-size feed chunks before `TerminalState::feed`, preserving bounded ingest semantics without dropping burst output; (2) batch overflow threshold is tied to core ingest cap (`4 * MAX_FEED_BYTES_PER_CALL`) to bound memory while preserving throughput; (3) CI/CodeQL/Scorecard use `paths-ignore` for docs/planning/assets-only changes, explicit job timeouts, and prebuilt `cargo-audit` installation via `taiki-e/install-action@cargo-audit` to reduce wall-clock and transient install failures. | Closed |
| G-015 | Terminal ingest hot path still allocated a new `Vec<CoreEvent>` per chunk, creating allocator churn risk during sustained AI CLI burst output; CI still spent avoidable cycles generating coverage when `CODECOV_TOKEN` is absent and CodeQL rebuild path lacked rust cache reuse. | Closed via convergence pass (2026-03-06): (1) `TerminalState::feed_into` introduced caller-provided event-buffer reuse and `feed` kept as compatibility wrapper; (2) GUI runtime ingest/notices now reuse a persistent scratch event buffer (`feed_events_scratch`) across chunks, reducing allocation churn in long sessions; (3) CI coverage workflow now gates heavy coverage generation/install steps on `CODECOV_TOKEN` presence; (4) CodeQL workflow now restores Rust cache and applies sparse/retry cargo network settings for lower rebuild overhead. | Closed |
| G-016 | Scorecard workflow still produced repeated SARIF upload warnings due non-URI placeholder artifact paths (`\"no file associated with this alert\"`) in generated results and remained sensitive to OSSF publish-path workflow verification constraints. | Closed via CI hygiene pass (2026-03-06): Scorecard now runs with `publish_results: false` (no OSSF publish verification dependency), normalizes placeholder URIs inline to valid relative form (`no-file-associated-with-this-alert`), and uploads normalized SARIF to code scanning in the same job with least-privilege permissions. | Closed |
| G-017 | Residual consistency gaps remained across runtime memory safety and CI governance: scrollback had line-only cap (no byte budget), parser still allocated action vectors on each chunk, foundation window event queue was uncapped, diagnostics retained legacy dead code paths, and CI/release workflows drifted from manual-governance + deterministic lock expectations. | Closed via consistency hardening pass (2026-03-06): (1) core scrollback now enforces dual bounds (50_000 lines + default 512 MiB byte budget) with oldest-line eviction; (2) parser adds `feed_into`/`resync_after_truncation_into` and terminal state reuses parser action scratch to reduce allocator churn; (3) foundation window queue is now hard-capped and coalesces high-frequency event kinds to prevent unbounded growth; (4) diagnostics feature removed legacy untyped dead paths and keeps typed payload contract as the single maintained API; (5) keypad mode is now explicit terminal state instead of silent no-op; (6) CI commands use deterministic `--locked` and stricter clippy scope, planning/docs changes are covered by dedicated `planning.yml`, and release workflow is manual-dispatch only with explicit `ref/tag` inputs. | Closed |
| G-018 | Post-hardening audit still identified consistency risks: dropped redraw event could remain unarmed after queue overflow, optional DECSTBM bottom (`CSI top; r`) parsed with wrong semantics, oversized scrollback line could evict historical lines under byte-cap mode, release workflow had ref/tag provenance ambiguity and no enforced preflight, and `R-08` evidence contract was not fully synchronized across traceability/manual artifacts. | Closed via audit-fix pass (2026-03-06): (1) window queue overflow now re-arms redraw when dropped and preserves `CloseRequested` under pressure with dedicated regression tests; (2) parser DECSTBM now treats empty bottom parameter as `None` to preserve terminal default-bottom semantics; (3) scrollback now drops oversized incoming line without evicting existing history and keeps deep-history default with 512 MiB byte budget; (4) release workflow adds strict preflight (planning validate + locked quality gates + MVP matrix), ref/tag integrity validation, and publish target SHA binding; (5) traceability/manual/release/start-readiness docs now explicitly encode dual-bound scrollback evidence for `R-08`. | Closed |

## Rule

Every new planning inconsistency discovered in v1.0 must be recorded here with explicit closure action and status.
